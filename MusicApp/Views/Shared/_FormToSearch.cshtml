  
@using (Ajax.BeginForm("Player", "Player",
         new { partialname = Model.FormPartialName },
         new AjaxOptions
         {
             HttpMethod = "POST",
             UpdateTargetId = "update",
             LoadingElementId = "loading",
             OnBegin = "HideResults",
             OnComplete = "ShowResults",
             OnFailure = "ShowError"
         },
         new {@id ="formtosearching"}

         ))
{
         <input style="color:black" name="search" type="text" />
         <button class="btn btn-primary btn-sm " type="submit">Szukaj</button>
     }
